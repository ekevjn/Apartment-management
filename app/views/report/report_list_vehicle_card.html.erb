<!DOCTYPE html>
<html>
<head>
  <title>Welcome to my site</title>
  <meta charset='utf-8' />
  <%= stylesheet_link_tag wicked_pdf_asset_base64("bootstrap-sass-official")  %>
</head>
<body>
  <div class="container">
    <div class="row">
      <div class="col-sm-12">
        <div class="text-center">
          <h3 class="text-center">CÔNG HÒA XÃ HỘI CHỦ NGHĨA VIÊT NAM</h3>
          <h6 class="text-center" style="text-decoration: underline;">Độc lập - Tự do - Hạnh phúc</h6>
        </div>
        <div class="text-center">
          <h1>DANH SÁCH ĐĂNG KÝ GỬI XE</h1>
        </div>
      </div>
    </div>

    <div class="table-responsive">
      <table class="table table-bordered table-hover pretty">
        <thead>
          <tr>
          <th width="2%">ID</th>
            <th width="7%">Mã số thẻ</th>
            <th width="7%">Số xe</th>
            <th width="10%">Loại xe</th>
            <th width="10%">Mặt bằng</th>
            <th width="15%">Trạng thái</th>
            <th width="10%">Ngày đăng ký</th>
          </tr>
        </thead>
        <tbody>
          <% @vehicle_cards.each do |vehicle_card| %>
          <tr>
            <td><%= vehicle_card.id %></td>
            <td><%= vehicle_card.card_no %></td>
            <td><%= vehicle_card.license_no %></td>
            <td><%= vehicle_card.vehicle_type %></td>
            <td><%= vehicle_card.ground_name %></td>
            <td><%= vehicle_card.status %></td>
            <td><%= vehicle_card.registered_time.strftime("%d/%m/%Y") if vehicle_card.registered_time.present? %></td>
          </tr>
          <% end %>
        </tbody>
      </table>

  </div>
</body>
</html>
