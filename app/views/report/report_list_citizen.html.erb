<!DOCTYPE html>
<html>
<head>
  <title>Welcome to my site</title>
  <meta charset='utf-8' />
  <%= stylesheet_link_tag wicked_pdf_asset_base64("bootstrap-sass-official")  %>
</head>
<body>
  <div class="container">
    <div class="row">
      <div class="col-sm-12">
        <div class="text-center">
          <h3 class="text-center">CÔNG HÒA XÃ HỘI CHỦ NGHĨA VIÊT NAM</h3>
          <h6 class="text-center" style="text-decoration: underline;">Độc lập - Tự do - Hạnh phúc</h6>
        </div>
        <div class="text-center">
          <h1>DANH SÁCH CƯ DÂN TRONG CHUNG CƯ</h1>
        </div>
      </div>
    </div>

    <table class="table table-bordered table-hover pretty">
      <thead>
        <tr>
          <th width="2%">STT</th>
          <th width="15%">Tên</th>
          <th width="10%">Căn hộ</th>
          <th width="25%">Email</th>
          <th width="5%">Giới tính</th>
          <th width="10%">Quốc tịch</th>
          <th width="10%">Ngày tham gia</th>
        </tr>
      </thead>
      <tbody>
        <% @citizens.each do |citizen| %>
        <tr>
          <td><%= citizen.id %></td>
          <td><%= citizen.name %></td>
          <td><%= citizen.ground.name if citizen.ground_id %></td>
          <td><%= citizen.email %></td>
          <td><%= (citizen.gender == 1 ? 'Nam' : 'Nữ') %></td>
          <td><%= citizen.nationality %></td>
          <td><%= citizen.created_at.strftime("%d/%m/%Y") %></td>
        </tr>
        <% end %>
      </tbody>
    </table>


  </div>
</body>
</html>
