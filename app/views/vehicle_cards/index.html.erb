<%= render 'layouts/sidebarmanager' %>
<%= render 'layouts/topnav' %>
<div class="right_col" role="main" style="min-height: 1575px;">
  <div id="layout-messages">
    <%= render 'layouts/messages' %>
  </div>
  <div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
      <%= render partial: 'layouts/subdomain_navigation', locals: { type: "vehicle_card", path: "vehicle_cards_path", new_path: "new_vehicle_card_path",
        export_path: "export_vehicle_cards_path", import_path: "import_vehicle_cards_path"} %>
        <hr/>
        <div id="vehicle_cards">
          <%= render 'vehicle_cards' %>
        </div>
      </div>
    </div>
  </div>

